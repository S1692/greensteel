# Frontend 환경변수 설정
# 이 파일을 .env.local로 복사하여 사용하세요

# Gateway URL (Railway 배포 후 실제 도메인으로 변경)
# 주의: localhost, 127.0.0.1, http://*3000 등은 절대 사용하지 마세요
NEXT_PUBLIC_GATEWAY_URL=https://your-gateway.railway.app

# 애플리케이션 설정
NEXT_PUBLIC_ENV=production
NEXT_PUBLIC_APP_NAME=greensteel
NEXT_PUBLIC_APP_VERSION=1.0.0

# 기능 활성화 설정
NEXT_PUBLIC_ENABLE_LCA=true
NEXT_PUBLIC_ENABLE_CBAM=true
NEXT_PUBLIC_ENABLE_DATA_UPLOAD=true

# 인증 관련 설정
# 이 값들은 프론트엔드에서 자동으로 처리됩니다
# - auth_token: 로그인 시 자동 저장
# - refresh_token: 토큰 갱신 시 사용
# - user_email: 사용자 식별용

# Google Analytics
NEXT_PUBLIC_GA_ID=G-2GFHCRYLT8

# 주의사항:
# 1. localhost, 127.0.0.1, http://*3000 등은 절대 사용하지 마세요
# 2. 프로덕션에서는 Railway의 퍼블릭 도메인만 사용하세요
# 3. Gateway를 통해서만 마이크로서비스에 접근하세요
# 4. NEXT_PUBLIC_ 접두사가 붙은 변수만 클라이언트에서 접근 가능합니다
# 5. 인증 토큰은 localStorage에 자동으로 저장/관리됩니다
